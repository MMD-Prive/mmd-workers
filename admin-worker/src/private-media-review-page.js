// Generated from webflow/internal/admin/mmd-review/review.{html,css,js}; keep source sections in sync.
const html = "<main id=\"mmd-review\" data-mmd-color-lock data-mmd-headline-gradient=\"off\" data-mmd-type-skip>\n  <nav class=\"mr-nav\" aria-label=\"Admin\"><a href=\"/internal/admin/dashboard\">← หน้าหลักแอดมิน</a><span>MMD PRIVÉ · ADMIN</span></nav>\n  <header class=\"mr-header\"><div><p class=\"mr-eyebrow\">PRIVATE MEDIA</p><h1>MMD Review</h1><p class=\"mr-muted\">ตรวจสื่อส่วนตัวก่อนเปิดให้ลูกค้าที่ได้รับสิทธิ์</p></div><span class=\"mr-badge\">สำหรับแอดมิน</span></header>\n  <section class=\"mr-toolbar\" aria-label=\"เลือกคิว\"><label for=\"mr-filter\">สถานะ <select id=\"mr-filter\"><option value=\"pending_review\">รอตรวจ</option><option value=\"approved\">อนุมัติแล้ว</option><option value=\"rejected\">ปฏิเสธ / เพิกถอนแล้ว</option></select></label><button type=\"button\" id=\"mr-refresh\">โหลดใหม่</button></section>\n  <p id=\"mr-status\" role=\"status\" aria-live=\"polite\">กำลังตรวจสิทธิ์และโหลดคิว…</p>\n  <section id=\"mr-list\" class=\"mr-list\" aria-label=\"รายการสื่อ\"></section>\n  <button type=\"button\" id=\"mr-more\" hidden>โหลดรายการถัดไป</button>\n  <aside class=\"mr-help\"><strong>อนุมัติสื่อ ≠ เปิดสิทธิ์ลูกค้า</strong><p>การอนุมัติจะทำให้สื่อพร้อมสำหรับ Private Pic / Private Clip เท่านั้น ลูกค้ายังต้องผ่านการยืนยันและได้รับสิทธิ์แยกต่างหาก</p></aside>\n  <dialog id=\"mr-dialog\" aria-labelledby=\"mr-title\">\n    <div class=\"mr-dialog-head\"><div><p class=\"mr-eyebrow\">ตรวจสื่อส่วนตัว</p><h2 id=\"mr-title\">Private media</h2><p id=\"mr-detail\" class=\"mr-muted\"></p></div><button type=\"button\" id=\"mr-close\" aria-label=\"ปิดการตรวจ\">ปิด</button></div>\n    <div id=\"mr-media\" class=\"mr-media\"></div><p id=\"mr-review-status\" role=\"status\" aria-live=\"polite\"></p>\n    <form id=\"mr-form\"><label class=\"mr-check\"><input type=\"checkbox\" id=\"mr-confirm\"> ฉันตรวจสื่อชิ้นนี้แล้ว และยืนยันผลที่เลือก</label><label class=\"mr-check\" id=\"mr-teaser-row\"><input type=\"checkbox\" id=\"mr-teaser\"> อนุมัติให้ใช้เป็น Private Teaser ก่อนจอง</label><p class=\"mr-muted\" id=\"mr-teaser-help\">เลือกเฉพาะสื่อที่ตั้งใจให้ลูกค้า LINE-verified ดูก่อนจอง · ไม่ทำให้เป็นสื่อ Public</p><label for=\"mr-note\">บันทึกการตรวจ <span class=\"mr-muted\">(ต้องระบุเมื่อปฏิเสธหรือเพิกถอน)</span></label><textarea id=\"mr-note\" rows=\"3\" maxlength=\"1000\" placeholder=\"เหตุผลหรือสิ่งที่ต้องแก้ไข\"></textarea><div class=\"mr-actions\"><button type=\"button\" id=\"mr-approve\" class=\"mr-primary\" disabled>อนุมัติสื่อ</button><button type=\"button\" id=\"mr-reject\" class=\"mr-danger\" disabled>ปฏิเสธ</button><button type=\"button\" id=\"mr-revoke\" class=\"mr-danger\" disabled hidden>เพิกถอนการอนุมัติ</button></div></form>\n  </dialog>\n</main>\n";
const css = "#mmd-review{--mr-bg:#101312;--mr-panel:#191e1b;--mr-line:#394138;--mr-text:#f5f1e7;--mr-muted:#c2c6bc;--mr-gold:#e7c988;box-sizing:border-box;min-height:100vh;padding:24px max(20px,calc((100vw - 1120px)/2));background:var(--mr-bg);color:var(--mr-text);font:16px/1.65 -apple-system,BlinkMacSystemFont,\"Segoe UI\",Tahoma,sans-serif;color-scheme:dark}\n#mmd-review *,#mmd-review *::before,#mmd-review *::after{box-sizing:border-box}\n#mmd-review h1{font-size:clamp(30px,5vw,42px);line-height:1.2;letter-spacing:-.03em;margin:6px 0 10px;color:var(--mr-text)}\n#mmd-review h2{font-size:21px;line-height:1.4;margin:0;color:var(--mr-text);overflow-wrap:anywhere}\n#mmd-review p{margin:8px 0;color:var(--mr-text)}\n#mmd-review a{color:var(--mr-gold);text-decoration:none}\n#mmd-review a:hover{text-decoration:underline}\n#mmd-review .mr-nav,#mmd-review .mr-header,#mmd-review .mr-toolbar,#mmd-review .mr-dialog-head{display:flex;justify-content:space-between;align-items:center;gap:16px;flex-wrap:wrap}\n#mmd-review .mr-nav{font-size:13px;color:var(--mr-muted);padding-bottom:26px;border-bottom:1px solid var(--mr-line)}\n#mmd-review .mr-header{padding:28px 0 24px}\n#mmd-review .mr-eyebrow{color:var(--mr-gold);font-size:12px;font-weight:700;letter-spacing:.13em}\n#mmd-review .mr-muted{color:var(--mr-muted);font-size:14px}\n#mmd-review .mr-badge{border:1px solid var(--mr-line);border-radius:30px;padding:6px 12px;color:var(--mr-muted);font-size:13px}\n#mmd-review .mr-toolbar{padding:16px 0;border-top:1px solid var(--mr-line);border-bottom:1px solid var(--mr-line)}\n#mmd-review label{color:var(--mr-text);display:block}\n#mmd-review button,#mmd-review select,#mmd-review textarea{font:inherit;color:var(--mr-text);background:#242b25;border:1px solid #596253;border-radius:10px;min-height:44px;padding:9px 14px}\n#mmd-review button{cursor:pointer;font-weight:600}\n#mmd-review button:hover:not(:disabled){border-color:var(--mr-gold);background:#30392f}\n#mmd-review button:disabled{cursor:not-allowed;opacity:.45}\n#mmd-review :focus-visible{outline:3px solid var(--mr-gold);outline-offset:3px}\n#mmd-review select{margin-left:8px}\n#mmd-review #mr-status{color:var(--mr-muted);padding:14px 0}\n#mmd-review .mr-list{display:grid;gap:12px}\n#mmd-review .mr-card{display:flex;gap:18px;align-items:center;justify-content:space-between;padding:20px;border:1px solid var(--mr-line);border-radius:14px;background:var(--mr-panel);min-width:0}\n#mmd-review .mr-card-info{min-width:0;flex:1}\n#mmd-review .mr-card h2{font-size:17px}\n#mmd-review .mr-kind{font-size:12px;color:var(--mr-gold);font-weight:700;letter-spacing:.05em}\n#mmd-review .mr-card button{flex-shrink:0}\n#mmd-review .mr-empty{padding:42px 20px;text-align:center;border:1px dashed var(--mr-line);border-radius:14px;color:var(--mr-muted)}\n#mmd-review #mr-more{margin-top:20px}\n#mmd-review .mr-help{margin-top:32px;padding:20px;border-left:2px solid var(--mr-gold);background:var(--mr-panel);color:var(--mr-text)}\n#mmd-review .mr-help p{color:var(--mr-muted);font-size:14px;margin-bottom:0}\n#mmd-review dialog{width:min(780px,calc(100vw - 24px));max-height:92vh;padding:24px;border:1px solid var(--mr-line);border-radius:18px;background:var(--mr-bg);color:var(--mr-text);overflow:auto}\n#mmd-review dialog::backdrop{background:#000c}\n#mmd-review .mr-media{display:flex;justify-content:center;align-items:center;min-height:180px;background:#060807;border-radius:12px;margin-top:20px;overflow:hidden}\n#mmd-review .mr-media img,#mmd-review .mr-media video{display:block;max-width:100%;max-height:46vh;object-fit:contain}\n#mmd-review .mr-check{display:flex;gap:12px;align-items:center;margin:18px 0}\n#mmd-review input[type=checkbox]{width:20px;height:20px;flex-shrink:0;accent-color:var(--mr-gold)}\n#mmd-review textarea{display:block;width:100%;resize:vertical;margin:8px 0 16px}\n#mmd-review .mr-actions{display:flex;flex-wrap:wrap;gap:10px}\n#mmd-review .mr-primary{background:var(--mr-gold);color:#171d16;border-color:var(--mr-gold)}\n#mmd-review .mr-primary:hover:not(:disabled){background:#f6dda8}\n#mmd-review .mr-danger{color:#ffc1b7;border-color:#8c594f}\n#mmd-review [hidden]{display:none!important}\n@media(max-width:540px){#mmd-review{padding:18px}#mmd-review .mr-card{flex-wrap:wrap}#mmd-review .mr-card button{width:100%}#mmd-review .mr-nav span{display:none}#mmd-review dialog{padding:18px}#mmd-review .mr-actions button{width:100%}}\n/* FINAL MMD CONTRAST SAFETY LAYER */\n#mmd-review h1,#mmd-review h2,#mmd-review label,#mmd-review strong{color:var(--mr-text)!important;-webkit-text-fill-color:var(--mr-text)!important;background-image:none!important}\n#mmd-review .mr-muted,#mmd-review .mr-help p,#mmd-review #mr-status{color:var(--mr-muted)!important;-webkit-text-fill-color:var(--mr-muted)!important}\n#mmd-review .mr-eyebrow,#mmd-review .mr-kind,#mmd-review a{color:var(--mr-gold)!important;-webkit-text-fill-color:var(--mr-gold)!important}\n#mmd-review button{-webkit-text-fill-color:currentColor!important;background-image:none!important}\n";
const js = "(function () {\n  'use strict';\n  var root = document.getElementById('mmd-review');\n  if (!root || root.dataset.ready) return;\n  root.dataset.ready = 'true';\n  var q = function (id) { return root.querySelector('#mr-' + id); };\n  var dialog = q('dialog'), selected = null, objectUrl = '', sha = '', busy = false, loading = false, cursor = null, generation = 0;\n  var controllers = new Set();\n  var labels = { pending_review: 'รอตรวจ', approved: 'อนุมัติแล้ว', rejected: 'ปฏิเสธ / เพิกถอนแล้ว' };\n  function element(tag, cls, text) { var el = document.createElement(tag); if (cls) el.className = cls; if (text) el.textContent = text; return el; }\n  function message(id, text) { q(id).textContent = text; }\n  function errorText(error) {\n    if (error.status === 401) return 'เซสชันหมดอายุ กรุณาเข้าสู่ระบบแอดมินอีกครั้ง';\n    if (error.status === 403) return 'บัญชีนี้ไม่มีสิทธิ์ดำเนินการ';\n    if (error.status === 409) return 'รายการเปลี่ยนแปลงแล้ว กรุณาโหลดคิวและตรวจใหม่';\n    return 'ยังยืนยันผลไม่ได้ กรุณาโหลดใหม่เพื่อตรวจสถานะล่าสุด';\n  }\n  async function api(path, body) {\n    var controller = new AbortController(); controllers.add(controller);\n    var timer = setTimeout(function () { controller.abort(); }, 30000);\n    try {\n      var response = await fetch('/v1/admin/private-media' + path, { method: body ? 'POST' : 'GET', credentials: 'same-origin', cache: 'no-store', headers: body ? { 'Content-Type': 'application/json' } : { Accept: 'application/json' }, body: body ? JSON.stringify(body) : undefined, signal: controller.signal });\n      if (!response.ok) { var err = new Error('request_failed'); err.status = response.status; throw err; }\n      if (path === '/file') {\n        var digest = response.headers.get('x-mmd-media-sha256') || '';\n        if (!/^[a-f0-9]{64}$/.test(digest)) throw new Error('unverified_media');\n        var blob = await response.blob();\n        if (!['image/jpeg', 'image/png', 'image/webp', 'video/mp4'].includes(blob.type) || !blob.size || blob.size > 50 * 1024 * 1024) throw new Error('invalid_media');\n        return { blob: blob, sha: digest };\n      }\n      var data = await response.json();\n      if (data.ok !== true) throw new Error('invalid_response');\n      return data;\n    } finally { clearTimeout(timer); controllers.delete(controller); }\n  }\n  function controls() {\n    var allowed = !!selected && !!sha && !busy && q('confirm').checked;\n    q('approve').disabled = !allowed;\n    q('reject').disabled = !allowed || !q('note').value.trim();\n    q('revoke').disabled = !allowed || !q('note').value.trim();\n    q('close').disabled = busy; q('note').disabled = busy; q('confirm').disabled = busy; q('teaser').disabled = busy || !selected || selected.status !== 'pending_review';\n  }\n  function clearMedia() {\n    generation++; sha = ''; selected = null;\n    var video = q('media').querySelector('video');\n    if (video) { video.pause(); video.removeAttribute('src'); video.load(); }\n    q('media').replaceChildren();\n    if (objectUrl) URL.revokeObjectURL(objectUrl);\n    objectUrl = ''; q('note').value = ''; q('confirm').checked = false; q('teaser').checked = false; controls();\n  }\n  function closeReview() { if (busy) return; clearMedia(); if (dialog.open) dialog.close(); }\n  async function openReview(item) {\n    clearMedia(); selected = item; var version = generation;\n    message('title', item.name); message('detail', (item.model_name || item.model_id) + ' · ' + labels[item.status]);\n    message('review-status', 'กำลังตรวจไฟล์และโหลดสื่อ…');\n    q('approve').hidden = item.status !== 'pending_review'; q('reject').hidden = item.status !== 'pending_review'; q('revoke').hidden = item.status !== 'approved';\n    q('teaser-row').hidden = item.status !== 'pending_review'; q('teaser-help').hidden = item.status !== 'pending_review'; q('teaser').checked = item.status === 'pending_review' && item.teaser_safe === true;\n    dialog.showModal(); controls();\n    try {\n      var result = await api('/file', { model_id: item.model_id, media_asset_id: item.id });\n      if (version !== generation || !dialog.open) return;\n      var media = element(result.blob.type === 'video/mp4' ? 'video' : 'img');\n      if (media.tagName === 'VIDEO') { media.controls = true; media.playsInline = true; media.disablePictureInPicture = true; media.setAttribute('controlsList', 'nodownload noremoteplayback'); }\n      else media.alt = 'สื่อส่วนตัวสำหรับตรวจโดยแอดมิน';\n      media.addEventListener(media.tagName === 'VIDEO' ? 'loadeddata' : 'load', function () { if (version !== generation) return; sha = result.sha; message('review-status', 'ตรวจสื่อให้ครบก่อนเลือกผลการตรวจ'); controls(); }, { once: true });\n      media.addEventListener('error', function () { sha = ''; message('review-status', 'เปิดสื่อไม่ได้ กรุณาปิดแล้วลองตรวจใหม่'); controls(); });\n      objectUrl = URL.createObjectURL(result.blob); media.src = objectUrl; q('media').replaceChildren(media);\n    } catch (error) { if (version === generation) { message('review-status', errorText(error)); controls(); } }\n  }\n  function card(item) {\n    var article = element('article', 'mr-card'), info = element('div', 'mr-card-info');\n    info.append(element('p', 'mr-kind', item.kind === 'private_clip' ? 'PRIVATE CLIP' : 'PRIVATE PIC'), element('h2', '', item.name));\n    var date = new Date(item.uploaded_at), when = Number.isNaN(date.getTime()) ? '' : date.toLocaleString('th-TH', { timeZone: 'Asia/Bangkok', dateStyle: 'medium', timeStyle: 'short' });\n    info.append(element('p', 'mr-muted', (item.model_name || item.model_id || 'ไม่พบ Model') + (when ? ' · ' + when : '')), element('p', 'mr-muted', labels[item.status] + (item.teaser_safe ? ' · PRIVATE TEASER' : '') + ' · ' + (Number(item.size) / 1024 / 1024).toFixed(1) + ' MB'));\n    article.append(info);\n    if (item.reviewable && item.status !== 'rejected') { var button = element('button', '', item.status === 'approved' ? 'ตรวจ / เพิกถอน' : 'เปิดตรวจ'); button.type = 'button'; button.addEventListener('click', function () { openReview(item); }); article.append(button); }\n    else if (!item.reviewable) info.append(element('p', 'mr-muted', 'ล็อกไว้: ต้องอัปโหลดผ่าน Private Media ใหม่'));\n    return article;\n  }\n  async function load(more) {\n    if (loading) return;\n    loading = true; q('refresh').disabled = true; q('filter').disabled = true; q('more').disabled = true;\n    if (!more) { cursor = null; q('list').replaceChildren(); }\n    message('status', 'กำลังโหลดคิว…');\n    try {\n      var params = new URLSearchParams({ status: q('filter').value }); if (more && cursor) params.set('cursor', cursor);\n      var data = await api('?' + params.toString());\n      if (!Array.isArray(data.items)) throw new Error('invalid_queue');\n      data.items.forEach(function (item) { q('list').append(card(item)); }); cursor = data.next_cursor || null;\n      var count = q('list').children.length;\n      message('status', count ? 'แสดง ' + count + ' รายการ' + (cursor ? ' · มีรายการเพิ่มเติม' : '') : 'ไม่มีรายการในสถานะนี้');\n      if (!count) q('list').append(element('div', 'mr-empty', 'เมื่อมีสื่อในสถานะนี้ รายการจะแสดงที่นี่'));\n    } catch (error) {\n      message('status', errorText(error));\n      if (error.status === 401) { var link = element('a', '', 'เข้าสู่ระบบแอดมิน'); link.href = '/internal/admin/login?next=%2Finternal%2Fadmin%2Fmmd-review'; q('list').replaceChildren(link); }\n    } finally { loading = false; q('refresh').disabled = false; q('filter').disabled = false; q('more').disabled = false; q('more').hidden = !cursor; }\n  }\n  async function decide(decision) {\n    if (busy || !selected || !sha || !q('confirm').checked) return;\n    if (decision !== 'approve' && !q('note').value.trim()) return;\n    var item = selected, teaserSafe = decision === 'approve' && q('teaser').checked; busy = true; controls(); message('review-status', 'กำลังบันทึกผลการตรวจ…');\n    try {\n      await api('/decision', { model_id: item.model_id, media_asset_id: item.id, expected_status: item.status, media_sha256: sha, decision: decision, teaser_safe: teaserSafe, note: q('note').value.trim() });\n      busy = false; closeReview(); await load(false);\n      message('status', decision === 'approve' ? (teaserSafe ? 'อนุมัติเป็น Private Teaser แล้ว · ลูกค้ายังต้องผ่านสิทธิ์ก่อนดู' : 'อนุมัติเป็นสื่อส่วนตัวแล้ว · ยังไม่ได้เปิดสิทธิ์ให้ลูกค้า') : decision === 'revoke' ? 'เพิกถอนการอนุมัติแล้ว' : 'ปฏิเสธสื่อแล้ว');\n    } catch (error) { busy = false; sha = ''; message('review-status', errorText(error)); controls(); }\n  }\n  q('refresh').addEventListener('click', function () { load(false); }); q('filter').addEventListener('change', function () { load(false); }); q('more').addEventListener('click', function () { load(true); });\n  q('close').addEventListener('click', closeReview); dialog.addEventListener('cancel', function (event) { event.preventDefault(); closeReview(); });\n  q('confirm').addEventListener('change', controls); q('teaser').addEventListener('change', controls); q('note').addEventListener('input', controls); q('form').addEventListener('submit', function (event) { event.preventDefault(); });\n  ['approve', 'reject', 'revoke'].forEach(function (decision) { q(decision).addEventListener('click', function () { decide(decision); }); });\n  function conceal() { controllers.forEach(function (controller) { controller.abort(); }); clearMedia(); if (dialog.open) dialog.close(); }\n  document.addEventListener('visibilitychange', function () { if (document.hidden) conceal(); });\n  window.addEventListener('pagehide', conceal);\n  load(false);\n}());\n";
export function renderPrivateMediaReview(nonce) {
 return '<!doctype html><html lang="th"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>MMD Review | Admin</title><style nonce="'+nonce+'">html,body{margin:0;background:#101312}'+css+'</style></head><body>'+html+'<script nonce="'+nonce+'">'+js+'</script></body></html>';
}
