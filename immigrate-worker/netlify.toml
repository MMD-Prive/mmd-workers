[build]
command = "echo \"Netlify LINE runtime retired; no production functions are deployed from immigrate-worker.\""

# No functions directory is configured. Source under netlify/functions is
# retained only as historical migration evidence and returns HTTP 410.
