{
  "name": "mmd-promotion-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/policy.js && node --check src/benefit-coordinator.js",
    "test": "node --test test/*.test.mjs"
  }
}
