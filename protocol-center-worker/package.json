{
  "name": "mmd-protocol-center-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.mjs",
    "deploy": "wrangler deploy --keep-vars"
  }
}
