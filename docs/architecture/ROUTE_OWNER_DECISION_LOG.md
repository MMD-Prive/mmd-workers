# Route Owner Decision Log

Status: Phase 1B ready for human review.
Base: `origin/main` at `dbc4669`

## Decision Principles Applied

- Webflow is preferred for public information and form surfaces.
- Worker page ownership is locked only with exact route evidence and a server-side reason.
- API ownership follows the Worker implementing the business contract.
- `mmd-redirect-worker` is deprecated and receives no new canonical ownership.
- `immigrate-worker` is legacy/temporary bridge only.
- Aliases stay compatibility-only until Phase 6 approval.
- Catch-all routes and workers.dev fallbacks are never canonical ownership.

## Resolution Matrix

| Conflict | Resolution | Chosen canonical route | Chosen owner | Rejected alternatives | Evidence | Reason | Risk | Implementation phase | Acceptance tests | Rollback dependency |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Booking page owner | RESOLVED | `/sigil/booking` | Route: `sigil-booking-proxy-worker`; UI source: Webflow | `mmd-redirect-worker` catch-all, direct stale Webflow as canonical route owner | `workers/sigil-booking-proxy-worker/*`, existing registry | Exact route config and proxy contract exist. | stale/default Webflow page | Phase 2B | apex/www redirect, sigil proxy, query allowlist, stale markers absent | keep proxy rollback to Webflow source |
| SIGIL apply owner | CORRECTED_UNRESOLVED | `/sigil/apply` | Candidate target: `sigil-worker`; current owner unresolved | Webflow fallback, Telegram brief page, catch-all, direct lock to handler not present | `mmd-redirect-worker/src/index.js` delegates; current `sigil-worker/src/index.js` has no matching handler and `sigil-worker/wrangler.toml` has no custom route | Review thread found delegation without target handler proof. | target not_found and private model route risk | Phase 4A | implement/prove GET/HEAD/slash/query, POST/OPTIONS/body/auth, owner headers | keep front gate and do not lock until handler and production route pass acceptance |
| Renewal route owner | CORRECTED_TEMPORARY | `/sigil/pay/renewal` | Candidate renderer: `member-dashboard-chat-worker`; production owner unverified | Webflow stale page, `member-pages-worker` manual legacy safety page, direct lock without route proof | current `member-dashboard-chat-worker/wrangler.toml` only declares LINE webhook routes; `mmd-redirect-worker/src/index.js` delegates SIGIL renewal to member pages; renderer code exists | Review thread found exact direct ownership not proven by current route config. | route precedence and production winner conflict | Phase 3/7 | exact route config, production winner, GET/HEAD/slash/query, Webflow bridge redirect | keep compatibility and do not remove front-gate/member-pages dependency until acceptance proof |
| Membership page owner | RESOLVED | `/member/membership` | `member-pages-worker` | Webflow, admin-worker, payment worker | `member-pages-worker/src/index.js`, `mmd-redirect-worker/src/index.js` | Worker page exists and does not own payment truth. | front gate dependency | Phase 3A | query preservation, no auto-renewal redirect | keep aliases |
| Payment evidence page owner | RESOLVED | `/pay/membership` | Page: `member-pages-worker`; API truth: `payments-worker` target | frontend activation, admin-only page | `member-pages-worker/src/index.js` | UI evidence submission is separate from payment truth. | API owner not fully enumerated | Phase 2/7 | proof does not activate membership; pending page works | keep admin review backend |
| Board page/API split | RESOLVED | `/sigil/board` page, `/sigil/board/runtime*` APIs | Page: Webflow; APIs: `sigil-board-worker` | `sigil-worker`, `immigrate-worker`, catch-all | registry, `workers/sigil-board-worker/*` | Visible route and API routes have different owners. | API Worker swallowing visible page | Phase 5/10 | visible page not JSON; runtime read-only; POST auth | keep Webflow page |
| Trust/login aliases | TEMPORARY_DECISION | `/sigil/start` | TBD Webflow target; current aliases via `mmd-redirect-worker` | independent alias pages | `mmd-redirect-worker/src/index.js` | Canonical visible owner not yet proven. | alias sprawl | Phase 2A then 6A | prove `/sigil/start`, query behavior | keep current aliases |
| Member dashboard owner | BLOCKED | `/member/dashboard` | TBD non-legacy owner | `immigrate-worker` canonical, generic recovery shell | `mmd-redirect-worker/src/index.js`, `immigrate-worker/src/index.ts` | Current owner is legacy bridge. | member status/auth truth risk | Phase 3A | login/session, `mmd_member_id`, dashboard state | retain front gate/legacy until replacement |
| Member application | BLOCKED | `/sigil/member/apply` | UI: Webflow; API owner TBD | `/member/apply` as canonical, frontend-only submission | `webflow/member/apply/*`, no backend route found | Backend `/v1/member/applications` not proven. | application data loss or frontend truth | Phase 3B | POST API, reference, auth/context | keep `/member/apply` alias |
| Recovery APIs | BLOCKED | `/sigil/recovery` family | TBD | simultaneous `mmd-care-intake-worker` and `sigil-worker` ownership | both source files contain recovery handling | Duplicate implementations. | private care data route collision | Phase 2A/6 | choose one owner, status/evidence API tests | do not change routes |
| Public model apply API overlap | BLOCKED | TBD | TBD between `sigil-worker` and `partners-worker` route families | locking both as canonical | `sigil-worker/src/index.js`, `partners-worker/src/index.ts` | Overlapping public-model API concepts. | write/API collision | Phase 4 | exact method/route/API contract tests | retain current routes |
| Public access API owner | CORRECTED_LOCKED_IMPLEMENTATION | `/public/api/access/intake` | `public-access-worker` | frontend-only public access truth; unowned `/public/api/*`; production-live claim from source alone | `public-access-worker/src/index.js`, `public-access-worker/wrangler.toml` | Exact handler and `sigil.mmdbkk.com/public/api/*` route config are repo-proven; only implemented subpaths are locked. | live acceptance still pending; unknown wildcard subpaths must fail closed | Phase 2A | live smoke POST/OPTIONS, origin behavior, evidence-only response, no access grant | no runtime change |
| Kenji Knowledge canonical route | CORRECTED_LOCKED | `/internal/admin/kenji-knowledge` | `admin-worker` | reversed SIGIL-prefixed canonical route; mixing with PR #209 | `admin-worker/src/index.js`, `admin-worker/wrangler.toml`, `admin-worker/kenji-knowledge-route.test.mjs` | Source constants and exact route config define non-SIGIL canonical route; SIGIL-prefixed route is legacy compatibility redirect. | legacy route removal before Phase 6 would break compatibility | Phase 5/6 | canonical GET/HEAD, legacy 308, query preservation, owner headers, no broad catch-all | keep legacy redirect |
| Admin namespace wildcard exceptions | CORRECTED_DEFAULT_WITH_EXCEPTIONS | `/v1/admin/*` default | `admin-worker` default; route-specific exceptions retain owners | blanket wildcard swallowing non-admin-worker contracts | `auth-worker/src/index.js`, `auth-worker/README.md`, current inventory scan for LIFF renewal queue | Route specificity wins over namespace default; `POST /v1/admin/access/grant` remains `auth-worker` until migrated/proxied; LIFF renewal queue remains an explicit exception candidate pending exact handler confirmation. | wildcard route migration could break implemented exception contracts | Phase 5 | enumerate exception owners, route attachment, auth, migration/proxy acceptance | no runtime change |
| Admin login owner | CORRECTED_LOCKED_IMPLEMENTATION | `/internal/admin/login` and `/internal/admin/login/session` | `admin-worker` | missing/unresolved admin login owner | `admin-worker/src/index.js`, `admin-worker/wrangler.toml`, `admin-worker/admin-login-session.test.mjs` | Exact route config and source implement GET/HEAD login page and POST session endpoint with HMAC-signed HttpOnly Secure SameSite=Lax cookie. | production smoke still pending; do not expose credentials in tests | Phase 5A | live-smoke login/session safely, verify host-only cookie attributes and no broad internal route | no runtime change |
| Generic `/sigil/pay` | DEFERRED_TO_PHASE | none | none | invented page owner | no evidence | No real implementation. | false canonical route | Phase 7 | implementation evidence required | none |
| Model console | BLOCKED | `/sigil/model/console` target | TBD | `/model/console` shell canonical | `mmd-redirect-worker` shell only | Current evidence is legacy shell. | model private data/auth risk | Phase 4B | auth, model session, brief access tests | keep shell until replacement |
| Admin visible pages | PARTIAL | internal route family | `admin-worker` for proven login/Kenji; TBD for remaining pages | migrate to `immigrate-worker`, unsigned cookies, or broad catch-all | admin API evidence plus exact login/Kenji source/config | Login/session and Kenji shell are implemented; remaining visible admin pages still need explicit route owner locks. | admin data/auth risk | Phase 5A/5B | signed session, auth, audit, exact route proof | no route changes |

## Phase 1B.5A Review Thread Corrections

| Decision | Affected routes | Previous decision | Review-thread finding | Repository evidence inspected | Corrected status | Corrected owner decision | Candidate owner | Production owner status | Acceptance required | Runtime change |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Renewal routes | `/sigil/pay/renewal`, `/pay/renewal` | `/sigil/pay/renewal` was LIVE CONNECTED/LOCKED to `member-dashboard-chat-worker`; `/pay/renewal` was LIVE CONNECTED/TEMPORARY. | Current route config does not prove direct member-dashboard-chat-worker ownership. | `member-dashboard-chat-worker/wrangler.toml` only declares LINE webhook routes; `mmd-redirect-worker/src/index.js` delegates SIGIL renewal to member pages; `member-pages-worker/src/index.js` renders manual legacy renewal; renderer/tests exist but are not route ownership proof. | `/sigil/pay/renewal` PARTIAL INTEGRATION; `/pay/renewal` UNRESOLVED. | TEMPORARY for SIGIL renewal, UNRESOLVED for pay renewal alias. | `member-dashboard-chat-worker` renderer candidate. | unverified until exact route attachment and production winner are proven. | Renewal route acceptance audit for exact routes, GET, HEAD, slash, query, and Webflow bridge. | none |
| SIGIL Apply routes | `/sigil/apply`, `/sigil/api/private-model/apply` | Both routes were LIVE CONNECTED/LOCKED to `sigil-worker`. | Current sigil-worker source lacks matching handlers and can fall through to `not_found`. | `sigil-worker/src/index.js` handles health, recovery, complaint evidence, and board APIs but not SIGIL apply/private-model apply; `sigil-worker/wrangler.toml` has no custom routes; `mmd-redirect-worker/src/index.js` delegates but delegation is not target-handler proof. | PARTIAL INTEGRATION. | UNRESOLVED. | `sigil-worker` candidate target only. | unverified until implementation and production acceptance pass. | Phase 4A Model Apply Acceptance for GET, HEAD, POST, OPTIONS, query, body preservation, auth, and owner headers. | none |

## Phase 1B.5B1 Second Review Thread Corrections

| Decision | Affected routes/namespaces | Previous decision | Review-thread finding | Repository evidence inspected | Corrected status | Corrected owner decision | Production verification | Acceptance required | Runtime change |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Public Access API | `/public/api/*`, `/public/api/access/intake` | Namespace was under-specified and still said Worker not proven in repo scan. | `public-access-worker` already has handler and route config for the intake API. | `public-access-worker/src/index.js`, `public-access-worker/wrangler.toml`, `public-access-worker/README.md`. | `POST /public/api/access/intake` API READY / UI MISSING; unknown subpaths not implemented. | `public-access-worker` LOCKED for repository implementation ownership only. | pending unless live smoke exists. | POST/OPTIONS live smoke, origin behavior, evidence-only response, no access grant. | none |
| Kenji Canonical Route | `/internal/admin/kenji-knowledge`, `/sigil/internal/admin/kenji-knowledge` | Canonical/legacy relationship was reversed. | Source constants make non-SIGIL path canonical and SIGIL-prefixed path legacy. | `admin-worker/src/index.js`, `admin-worker/wrangler.toml`, `admin-worker/kenji-knowledge-route.test.mjs`. | canonical route LIVE CONNECTED from source/config; legacy redirect retained. | `/internal/admin/kenji-knowledge` LOCKED to `admin-worker`; SIGIL path is compatibility redirect. | source/config verified; live smoke pending. | canonical GET/HEAD, legacy 308, query preservation, owner headers, no broad catch-all. | none |
| Admin Namespace Exceptions | `/v1/admin/*`, `POST /v1/admin/access/grant`, `/v1/admin/line/liff-renewal-queue` | Blanket namespace lock did not record exceptions. | Wildcard could swallow routes whose current contracts belong elsewhere. | `auth-worker/src/index.js`, `auth-worker/README.md`, repository scan for LIFF renewal queue. | default namespace LOCKED to `admin-worker`; exceptions explicit. | Route specificity wins; `auth-worker` grant remains exception until migrated/proxied; LIFF renewal queue remains exception candidate pending exact handler confirmation. | pending per endpoint. | enumerate exception owners/routes before wildcard migration. | none |
| Admin Login Owner | `/internal/admin/login`, `/internal/admin/login/session` | Admin login owner was unresolved/missing. | `admin-worker` source/config already implements login page and signed session flow. | `admin-worker/src/index.js`, `admin-worker/wrangler.toml`, `admin-worker/admin-login-session.test.mjs`. | source/config LIVE CONNECTED. | `admin-worker` LOCKED for repository implementation ownership. | source/config verified; live smoke pending. | safe live smoke for GET login and POST session; verify HMAC-signed HttpOnly Secure SameSite=Lax host-only cookie behavior. | none |

## Owner Decisions By Family

| Family | Decision |
| --- | --- |
| Public route family | Mostly UNRESOLVED/TEMPORARY pending Phase 2A evidence; no new owner invented. |
| Booking | `/sigil/booking` LOCKED to `sigil-booking-proxy-worker` route plus Webflow UI source; booking APIs TEMPORARY to `sigil-booking-worker` until exact endpoint acceptance. |
| Member | `/member/membership` LOCKED to `member-pages-worker`; dashboard/login/payments/application remain BLOCKED or UNRESOLVED. |
| Payment | `/pay/membership` and `/pay/pending-verification` remain LOCKED by current page evidence; `/sigil/pay/renewal` is TEMPORARY and `/pay/renewal` is UNRESOLVED pending exact renewal route proof; payment API namespaces TEMPORARY to `payments-worker` target until exact enumeration. |
| Model/apply | `/sigil/apply` and `/sigil/api/private-model/apply` are UNRESOLVED with `sigil-worker` as candidate target until matching handlers and production route attachment are proven; console/client brief/confirm routes BLOCKED. |
| Internal/admin | `/v1/admin/*` LOCKED to `admin-worker`; visible admin routes BLOCKED pending Phase 5. |
| SIGIL Board | Visible `/sigil/board` LOCKED to Webflow; runtime/actions/audit LOCKED to `sigil-board-worker`. |
| System APIs | Admin, realtime, Telegram locked; member/job/public/Kenji namespaces have explicit blockers. |

## Stop Line

No runtime implementation was performed.
No route mutation was performed.
No deploy or Webflow publish was performed.
