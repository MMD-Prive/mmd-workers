# TMIB ACT 001 rollback

If production access or media delivery fails after deploy, keep the Long Story fail-closed. The public teaser may remain available, but do not fall back to public protected-image URLs or browser-inferred membership/payment state. Roll back the Worker commit or temporarily disable the 299 THB purchase CTA until the canonical route is healthy.
