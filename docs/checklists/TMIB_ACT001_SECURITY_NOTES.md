# TMIB ACT 001 security boundary

The protected-image design prevents the published page from exposing the private frame source URLs and requires a verified LINE session plus short-lived identity-bound signatures for delivery. It is a deterrence and access-control layer, not DRM. Browser developer tools can inspect content already delivered to an authorized session, and operating-system screenshots or external cameras cannot be technically prevented by a normal web page.
