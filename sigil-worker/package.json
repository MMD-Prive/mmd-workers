{
  "name": "sigil-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --import ./test/setup.mjs --test test/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.50.0"
  }
}
