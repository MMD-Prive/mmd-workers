// Runs inside the Control Room closure. Private fields only enter saveVault().
export const PARTNER_OPERATIONS_JS = String.raw`
  var privateKinds={lock:"ล็อกคิว",travel:"เดินทาง",external:"งานนอก MMD",unavailable:"ไม่ว่าง"};
  var terminalJobs=["completed","cancelled","canceled","closed","void","declined"];
  var assetBlobs=[],coverBlobs=[];
  var statusLabels={review:"รอตรวจ",pending_review:"รอตรวจ",approved:"อนุมัติแล้ว",rejected:"ไม่อนุมัติ",superseded:"ฉบับเก่า",pending:"รอดำเนินการ",ready:"รอจ่าย",earned:"บันทึกรายได้แล้ว",paid:"จ่ายแล้ว",void:"ยกเลิกรายได้",voided:"ยกเลิกรายได้",refunded:"คืนเงินแล้ว",reversed:"กลับรายการ",disputed:"รอตรวจข้อโต้แย้ง",held:"พักการจ่าย",confirmed:"ยืนยันแล้ว",declined:"รับงานไม่ได้",changes_requested:"ขอแก้ไข",sent:"ส่งแล้ว",failed:"ส่งไม่สำเร็จ",unbound:"รอเชื่อม Telegram",archived:"เก็บเข้าประวัติ"};
  function label(value){return statusLabels[String(value||"").toLowerCase()]||value||"—";}
  function dayKey(value){var d=new Date(value);return isNaN(d.getTime())?"":new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Bangkok",year:"numeric",month:"2-digit",day:"2-digit"}).format(d);}
  function localInput(value){if(!value)return "";var d=new Date(value);return isNaN(d.getTime())?"":new Date(d.getTime()+7*3600000).toISOString().slice(0,16);}
  function bangkokIso(value){return value?new Date(value+":00+07:00").toISOString():null;}
  function modelById(id){return(state.data.models||[]).find(function(m){return m.model_record_id===id;});}
  function activeJob(job){return terminalJobs.indexOf(job.lifecycle_status)<0&&job.status!=="declined";}
  function interval(row){var start=Date.parse(row.start_at),end=Date.parse(row.end_at);return Number.isFinite(start)&&Number.isFinite(end)&&end>start?[start,end]:null;}
  function overlaps(a,b){var x=interval(a),y=interval(b);return !!(x&&y&&a.model_record_id&&a.model_record_id===b.model_record_id&&x[0]<y[1]&&y[0]<x[1]);}
  function privateEvents(){return state.vault&&Array.isArray(state.vault.events)?state.vault.events:[];}
  function allCalendarEvents(){return(state.data.jobs||[]).filter(activeJob).map(function(j){return Object.assign({event_id:j.session_record_id,kind:"mmd"},j);}).concat(privateEvents().filter(function(e){return !e.archived&&terminalJobs.indexOf(e.status)<0;}));}
  function conflictFor(row){if(row.archived||terminalJobs.indexOf(row.lifecycle_status||row.status)>=0)return [];return allCalendarEvents().filter(function(e){return (e.event_id||e.session_record_id)!==(row.event_id||row.session_record_id)&&overlaps(row,e);});}
  function modelOptions(includeAll){return(includeAll?'<option value="">ทุกโมเดล</option>':'<option value="">เลือกโมเดล</option>')+(state.data.models||[]).map(function(m){return '<option value="'+esc(m.model_record_id)+'">'+esc(m.display_name)+'</option>';}).join("");}
  function empty(message){return '<div class="pcr-empty">'+esc(message)+'</div>';}
  function ensurePrivate(){if(state.vault)return true;showView("privacy");setFlash("ปลดล็อก Private Vault เพื่อใช้ข้อมูลส่วนตัว","error");return false;}
  function downloadText(name,text,type){var url=URL.createObjectURL(new Blob([text],{type:type||"text/plain;charset=utf-8"}));var a=document.createElement("a");a.href=url;a.download=name;a.click();setTimeout(function(){URL.revokeObjectURL(url);},1000);}
  function csvCell(value){var s=String(value==null?"":value);if(/^[\s]*[=+@-]/.test(s))s="'"+s;return '"'+s.replace(/"/g,'""')+'"';}
  function exportRows(name,rows){downloadText(name,"\uFEFF"+rows.map(function(row){return row.map(csvCell).join(",");}).join("\r\n"),"text/csv;charset=utf-8");}
  function onButton(selector,fn){var el=$(selector,root);if(el)el.onclick=fn;}
  function section(name,title,copy,body){return '<section data-view-panel="'+name+'" hidden><header class="pcr-top"><div><p class="mmdp-eyebrow">SĪGIL PARTNER</p><h2>'+title+'</h2><p>'+copy+'</p></div></header>'+body+'</section>';}
  function installOperations(){
    var nav=$(".pcr-side",root);
    [["agreements","Agreements"],["performance","Performance"],["console","Console"],["activity","ประวัติ / แจ้งเตือน"]].forEach(function(item){var b=document.createElement("button");b.type="button";b.dataset.view=item[0];b.textContent=item[1];nav.insertBefore(b,$('[data-view="privacy"]',nav));b.onclick=function(){showView(item[0]);};});
    $(".pcr-main",root).insertAdjacentHTML("beforeend",
      section("agreements","Agreements","ข้อตกลงปัจจุบันและคำขอเปลี่ยนระบบรายโมเดล",'<div data-agreements></div>')+
      section("performance","Performance","สรุปจากงานและรายการรายได้ของคุณ",'<div data-performance></div>')+
      section("console","Partner Console","ประสานงานผ่านทีม MMD หรือเปิดช่องติดต่อโมเดลที่คุณบันทึกไว้",'<div class="pcr-section"><form data-console-form><label><span>งาน MMD</span><select name="session_record_id" required></select></label><label><span>ฝากข้อความถึงทีมเพื่อประสานลูกค้า / โมเดล</span><textarea name="message" maxlength="2000" required></textarea></label><label class="pcr-consent"><input type="checkbox" name="share_with_mmd" required><span>ฉันเลือกแชร์ข้อความนี้กับทีม MMD เพื่อประสานงาน</span></label><button type="submit">ส่งคำขอประสานงาน</button></form><div data-console-history></div></div><div class="pcr-section" data-model-contacts></div>')+
      section("activity","ประวัติ / แจ้งเตือน","คำขอที่แชร์กับ MMD และสถานะล่าสุดจากระบบ",'<div data-activity></div>'));
    $("[data-jobs]",root).insertAdjacentHTML("beforebegin",'<div class="pcr-toolbar"><label>โมเดล<select data-calendar-model></select></label><label>ตั้งแต่<input type="date" data-calendar-from></label><label>ถึง<input type="date" data-calendar-to></label><label>สถานะ<select data-calendar-status><option value="">ทั้งหมด</option><option value="pending">รอยืนยัน</option><option value="confirmed">ยืนยันแล้ว</option><option value="changes_requested">ขอแก้ไข</option><option value="declined">รับงานไม่ได้</option></select></label><button type="button" data-calendar-today>วันนี้</button><button type="button" data-calendar-clear>ทั้งหมด</button><button type="button" data-export-calendar>Export CSV</button></div><div data-calendar-warning></div>');
    $("[data-jobs]",root).insertAdjacentHTML("afterend",'<div class="pcr-section-head"><h3>คิวส่วนตัว / เดินทาง / งานภายนอก</h3><button type="button" data-event-add>+ เพิ่มคิว</button></div><p>ข้อมูลส่วนนี้เข้ารหัสบนอุปกรณ์ของ Partner ก่อนบันทึก และอยู่ภายใต้การควบคุมของ Partner</p><label><input type="checkbox" data-show-archived-events> แสดงคิวที่เก็บเข้าประวัติ</label><div data-private-events></div>');
    $("[data-models]",root).insertAdjacentHTML("beforebegin",'<div class="pcr-toolbar"><label>ค้นหาโมเดล<input type="search" data-model-search placeholder="ชื่อโมเดล"></label><label>แสดง<select data-roster-filter><option value="active">ในสังกัด</option><option value="archived">เก็บเข้าประวัติส่วนตัว</option><option value="all">ทั้งหมด</option></select></label></div>');
    $("[data-earnings]",root).insertAdjacentHTML("beforebegin",'<div class="pcr-toolbar"><button type="button" data-export-earnings>Export CSV</button><button type="button" data-print-earnings>พิมพ์ / บันทึก PDF</button></div>');
    $("[data-model-form] .pcr-form-grid",root).insertAdjacentHTML("beforeend",'<label class="wide"><span>ความพร้อม / เงื่อนไขที่เลือกแชร์</span><textarea name="availability_note" maxlength="1200"></textarea></label><fieldset><legend>ตารางใช้เรท · เวลาไทย</legend><label>รูปแบบ<select name="schedule_type"><option>Always</option><option>Date range</option><option>Date + time range</option></select></label><label>เริ่ม<input type="datetime-local" name="effective_from_at"></label><label>สิ้นสุด<input type="datetime-local" name="effective_until_at"></label></fieldset><fieldset><legend>ระบบทำงาน 1 · 2 · 3</legend><div data-current-working></div><div data-sales-preview></div><label>เสนอระบบ<select name="working_system"><option value="bridge">1 · Bridge (5–10%)</option><option value="co_partner">2 · Co-Partner (เรทรวม)</option><option value="profit_share">3 · Profit Share</option></select></label><label>Bridge (%)<input type="number" min="5" max="10" step="0.01" name="commission_percent"></label><label>Co-Partner เรทรวม (THB)<input type="number" min="0" max="1000000" name="source_rate_thb"></label><label>Profit Share ของ Partner (%)<input type="number" min="0.01" max="99.99" step="0.01" name="partner_share_percent"></label><label>เหตุผล / เงื่อนไข<textarea name="working_reason" maxlength="1200"></textarea></label><button type="button" data-submit-working>ส่งข้อตกลงให้ Boss Per ตรวจ</button></fieldset><fieldset><legend>รูปและไฟล์ที่แชร์แล้ว</legend><div data-model-assets></div></fieldset><fieldset class="pcr-private-field"><legend>ข้อมูลส่วนตัว · Partner controls access</legend><label>LINE ของโมเดล<input name="private_line" maxlength="120"></label><label>Telegram ของโมเดล<input name="private_telegram" maxlength="120" placeholder="@username"></label><label>สถานะส่วนตัว<select name="private_availability"><option value="">ยังไม่ระบุ</option><option value="available">พร้อมรับงาน</option><option value="busy">ติดคิว</option><option value="away">อยู่นอกกรุงเทพฯ</option><option value="off">พักรับงาน</option></select></label><button type="button" data-save-private-model>บันทึกข้อมูลส่วนตัว</button><button type="button" data-archive-private-model>เก็บ / คืนจากประวัติส่วนตัว</button></fieldset><fieldset><legend>ประวัติงานและรายได้ของโมเดล</legend><div data-model-history></div></fieldset>');
    $("[data-vault-open] .pcr-vault-actions",root).insertAdjacentHTML("beforeend",'<button type="button" data-lock-vault>ล็อกทันที</button><button type="button" data-export-vault>สำรองข้อมูลที่เข้ารหัส</button><label>ไฟล์สำรอง<input type="file" data-import-vault-file accept="application/json,.json"></label><label>PIN ของไฟล์สำรอง<input type="password" data-import-vault-pin autocomplete="off"></label><button type="button" data-import-vault>กู้คืนไฟล์สำรอง</button>');
    root.insertAdjacentHTML("beforeend",'<dialog class="pcr-dialog" data-event-dialog><div class="pcr-dialog-head"><h2>คิวส่วนตัว</h2><button type="button" data-close-event>×</button></div><form class="pcr-form" data-event-form><input type="hidden" name="event_id"><div class="pcr-form-grid"><label>โมเดล<select name="model_record_id" required></select></label><label>ประเภท<select name="kind"><option value="lock">ล็อกคิว</option><option value="travel">เดินทาง</option><option value="external">งานนอก MMD</option><option value="unavailable">ไม่ว่าง</option></select></label><label>เริ่ม · เวลาไทย<input name="start_at" type="datetime-local" required></label><label>สิ้นสุด · เวลาไทย<input name="end_at" type="datetime-local" required></label><label>พื้นที่<input name="area" maxlength="180"></label><label>สถานะ<select name="status"><option value="pending">ล็อกคิว</option><option value="confirmed">ยืนยันแล้ว</option><option value="completed">เสร็จแล้ว</option><option value="cancelled">ยกเลิก</option></select></label><label>ค่าตัวรับ (งานภายนอก)<input name="income" type="number" min="0" step="0.01"></label><label>ต้นทุน (งานภายนอก)<input name="cost" type="number" min="0" step="0.01"></label><label>ชำระแล้ว<select name="payment"><option value="pending">รอชำระ</option><option value="paid">ชำระแล้ว</option></select></label><label class="wide">โน้ตส่วนตัว<textarea name="note" maxlength="4000"></textarea></label></div><div class="pcr-form-actions"><button type="submit">เข้ารหัสและบันทึกคิว</button><button type="button" data-event-archive>เก็บคิวเข้าประวัติ</button></div><p data-event-error role="alert"></p></form></dialog>');
    $$('[data-calendar-model],[data-calendar-from],[data-calendar-to],[data-calendar-status]',root).forEach(function(el){el.onchange=renderCalendar;});
    $$('[data-model-search],[data-roster-filter]',root).forEach(function(el){el.oninput=filterRoster;});
    onButton('[data-calendar-today]',function(){var day=dayKey(new Date());$('[data-calendar-from]',root).value=day;$('[data-calendar-to]',root).value=day;renderCalendar();});
    onButton('[data-calendar-clear]',function(){$$('[data-calendar-from],[data-calendar-to],[data-calendar-status]',root).forEach(function(e){e.value="";});renderCalendar();});
    onButton('[data-show-archived-events]',renderCalendar);
    onButton('[data-event-add]',function(){openEvent();});
    onButton('[data-close-event]',function(){$('[data-event-dialog]',root).close();});
    $('[data-event-form]',root).onsubmit=submitEvent;
    onButton('[data-event-archive]',archiveEvent);
    onButton('[data-submit-working]',submitWorking);
    onButton('[data-save-private-model]',savePrivateModel);
    onButton('[data-archive-private-model]',archivePrivateModel);
    onButton('[data-lock-vault]',lockVault);
    onButton('[data-export-vault]',async function(){try{if(!ensurePrivate())return;var payload=Object.assign({},state.vault,{_partner_record_id:state.data.partner.id,general_note:$('[data-general-note]',root).value});var envelope=await encryptVaultPayload(payload,state.vaultPin,state.vaultSalt);downloadText("sigil-private-encrypted.json",JSON.stringify(envelope),"application/json");}catch(e){setFlash(e.message,"error");}});
    onButton('[data-import-vault]',importVaultBackup);
    onButton('[data-export-calendar]',exportCalendar);
    onButton('[data-export-earnings]',function(){exportRows("sigil-earnings.csv",[["งาน","โมเดล","ฐาน","ค่าตอบแทน","สถานะ","จ่ายเมื่อ"]].concat((state.data.commissions||[]).map(function(c){return[c.jobId,c.model,c.basisAmount,c.commission,label(c.status),c.paidAt||""];})));});
    onButton('[data-print-earnings]',function(){showView("earnings");window.print();});
    $('[data-console-form]',root).onsubmit=submitConsole;
  }
  function renderOperations(data){
    var selected=$('[data-calendar-model]',root).value;$('[data-calendar-model]',root).innerHTML=modelOptions(true);$('[data-calendar-model]',root).value=selected;
    renderCalendar();renderAgreements();renderActivity();renderPerformance();filterRoster();renderContacts();renderModelCovers();
    $('[data-console-form] select',root).innerHTML='<option value="">เลือกงานที่ต้องการประสาน</option>'+(data.jobs||[]).map(function(j){return '<option value="'+esc(j.session_record_id)+'">'+esc(j.model_name)+' · '+dateTime(j.start_at||j.date)+'</option>';}).join("");
  }
  function calendarMatch(row){var model=$('[data-calendar-model]',root).value,from=$('[data-calendar-from]',root).value,to=$('[data-calendar-to]',root).value,status=$('[data-calendar-status]',root).value;var day=dayKey(row.start_at||row.date);return(!model||row.model_record_id===model)&&(!from||day>=from)&&(!to||day<=to)&&(!status||row.status===status);}
  function renderCalendar(){
    var jobs=(state.data.jobs||[]).filter(calendarMatch).slice().sort(function(a,b){return Date.parse(a.start_at||a.date)-Date.parse(b.start_at||b.date);});
    $('[data-jobs]',root).innerHTML=jobs.length?jobs.map(function(j){var conflicts=conflictFor(j);return '<div>'+jobCard(j)+(conflicts.length?'<p class="pcr-collision">คิวชน '+conflicts.length+' รายการ · ตรวจตารางก่อนยืนยัน</p>':"")+'</div>';}).join(""):empty("ตารางพร้อมแสดงงานในช่วงที่เลือก");
    var events=privateEvents().filter(function(e){return (e.archived===true)===$('[data-show-archived-events]',root).checked&&calendarMatch(e);}).sort(function(a,b){return Date.parse(a.start_at)-Date.parse(b.start_at);});
    $('[data-private-events]',root).innerHTML=!state.vault?empty("ปลดล็อก Private Vault เพื่อดูคิวส่วนตัวและตรวจคิวชนกับงาน MMD"):events.length?events.map(function(e){var m=modelById(e.model_record_id),collisions=conflictFor(e);return '<article class="pcr-job"><div class="pcr-job-main"><div><span class="pcr-status">'+esc(privateKinds[e.kind])+'</span><h3>'+esc(m?m.display_name:"โมเดลเดิม")+'</h3></div><button type="button" data-edit-event="'+esc(e.event_id)+'">แก้ไข</button></div><p>'+dateTime(e.start_at)+' — '+dateTime(e.end_at)+'</p><p>'+esc(e.area)+' · '+esc(label(e.status))+'</p><p>'+esc(e.note)+'</p>'+(e.kind==="external"?'<p>รับ '+money(e.income)+' · ต้นทุน '+money(e.cost)+' · '+esc(label(e.payment))+'</p>':"")+(collisions.length?'<p class="pcr-collision">คิวชน '+collisions.length+' รายการ</p>':"")+'</article>';}).join(""):empty("ตารางส่วนตัวพร้อมเมื่อเพิ่มคิว");
    var unknown=(state.data.jobs||[]).filter(function(j){return activeJob(j)&&(!j.model_record_id||!interval(j));}).length;
    $('[data-calendar-warning]',root).textContent=unknown?"มี "+unknown+" งานที่ข้อมูลโมเดลหรือเวลาไม่ครบ จึงยังตรวจคิวชนให้ไม่ได้":"";
    $$('[data-edit-event]',root).forEach(function(b){b.onclick=function(){openEvent(b.dataset.editEvent);};});bindDynamic();
  }
  function openEvent(id){if(!ensurePrivate())return;var form=$('[data-event-form]',root);form.reset();form.elements.model_record_id.innerHTML=modelOptions(false);var e=privateEvents().find(function(x){return x.event_id===id;})||{};["event_id","model_record_id","kind","area","status","income","cost","payment","note"].forEach(function(k){if(e[k]!=null)form.elements[k].value=e[k];});form.elements.start_at.value=localInput(e.start_at);form.elements.end_at.value=localInput(e.end_at);$('[data-event-error]',root).textContent="";$('[data-event-archive]',root).hidden=!id;$('[data-event-archive]',root).textContent=e.archived?'คืนคิวจากประวัติ':'เก็บคิวเข้าประวัติ';$('[data-event-dialog]',root).showModal();}
  function submitEvent(event){event.preventDefault();if(!ensurePrivate())return;var f=event.currentTarget;var e={event_id:f.elements.event_id.value||uid("event_"),model_record_id:f.elements.model_record_id.value,kind:f.elements.kind.value,start_at:bangkokIso(f.elements.start_at.value),end_at:bangkokIso(f.elements.end_at.value),area:f.elements.area.value,status:f.elements.status.value,income:Number(f.elements.income.value||0),cost:Number(f.elements.cost.value||0),payment:f.elements.payment.value,note:f.elements.note.value,updated_at:new Date().toISOString()};if(!modelById(e.model_record_id)||!interval(e)||!Number.isFinite(e.income)||!Number.isFinite(e.cost)||e.income<0||e.cost<0){$('[data-event-error]',root).textContent="ตรวจโมเดล เวลาเริ่ม/สิ้นสุด และจำนวนเงินอีกครั้ง";return;}var collisions=conflictFor(e);if(collisions.length){$('[data-event-error]',root).textContent="คิวชน "+collisions.length+" รายการ กรุณาปรับเวลาให้ไม่ซ้อนก่อนบันทึก";return;}var before=privateEvents().slice();state.vault.events=before.filter(function(x){return x.event_id!==e.event_id;}).concat(e);var b=f.querySelector('button[type="submit"]');b.disabled=true;saveVault().then(function(){$('[data-event-dialog]',root).close();renderOperations(state.data);}).catch(function(err){state.vault.events=before;$('[data-event-error]',root).textContent=err.message;}).finally(function(){b.disabled=false;});}
  function archiveEvent(){if(!ensurePrivate())return;var id=$('[data-event-form]',root).elements.event_id.value;var e=privateEvents().find(function(x){return x.event_id===id;});if(!e)return;var before=e.archived===true;if(before&&conflictFor(Object.assign({},e,{archived:false})).length)return setFlash("คิวชน กรุณาปรับเวลาก่อนคืนจากประวัติ","error");e.archived=!before;saveVault().then(function(){$('[data-event-dialog]',root).close();renderOperations(state.data);}).catch(function(err){e.archived=before;setFlash(err.message,"error");});}
  function filterRoster(){var q=$('[data-model-search]',root).value.toLowerCase(),mode=$('[data-roster-filter]',root).value;$$('[data-model]',root).forEach(function(card){var m=modelById(card.dataset.model),privateData=state.vault&&state.vault.models&&state.vault.models[card.dataset.model]||{};if(!m)return;var archived=privateData.archived||m.roster_active===false;card.hidden=!(String(m.display_name).toLowerCase().includes(q)&&(mode==="all"||(mode==="archived"?archived:!archived)));});}
  function workingText(w){if(!w||w.status!=="approved"||!w.system)return "ข้อตกลงจะแสดงเมื่อการยืนยันเสร็จสมบูรณ์";var amount=w.system==="bridge"?w.commission_percent+"%":w.system==="co_partner"?money(w.source_rate_thb):w.partner_share_percent+"% หลังหักต้นทุนตามข้อตกลง";return w.label+" · "+amount+" · v"+w.version;}
  function renderAgreements(){var models=state.data.models||[];var terms='<article class="pcr-section"><h3>ข้อตกลง Partner</h3><p>'+esc(state.data.partner.terms_accepted?'ยอมรับแล้ว · '+state.data.partner.terms_version:'รอยืนยันข้อตกลง')+'</p><a href="/partner/terms?t='+encodeURIComponent(token)+'" rel="noreferrer">อ่านข้อตกลง Partner</a></article>';$('[data-agreements]',root).innerHTML=terms+(models.length?models.map(function(m){var w=m.working_system;return '<article class="pcr-section"><h3>'+esc(m.display_name)+'</h3><p>'+esc(workingText(w))+'</p><p>เริ่ม '+dateTime(w&&w.effective_from)+(w&&w.effective_until?' · สิ้นสุด '+dateTime(w.effective_until):"")+'</p>'+(w&&w.pending_change?'<p class="pcr-status">คำขอ v'+esc(w.pending_change.version)+' · '+esc(label(w.pending_change.status))+'</p>':"")+'<button type="button" data-open-agreement="'+esc(m.model_record_id)+'">ดู / เสนอเปลี่ยนข้อตกลง</button></article>';}).join(""):empty("รายชื่อที่มีข้อตกลงจะแสดงที่นี่"));$$('[data-open-agreement]',root).forEach(function(b){b.onclick=function(){openModelEditor(b.dataset.openAgreement);};});}
  function changeTitle(c){return({add_model:"เพิ่มโมเดล",update_profile:"แก้ไขโปรไฟล์",remove_model:"ถอดออกจากสังกัด",update_working_system:"ข้อตกลง",archive_asset:"เก็บรูปเข้าประวัติ",restore_asset:"คืนรูปจากประวัติ",set_cover:"เปลี่ยนภาพหลัก",console_request:"ประสานงาน"})[c.action]||c.action;}
  function renderActivity(){var changes=state.data.model_changes||[];var html=changes.map(function(c){var m=modelById(c.model_record_id);return '<article class="pcr-job"><b>'+esc(changeTitle(c))+' · '+esc(m?m.display_name:c.payload.display_name||"Partner")+'</b><p>'+esc(label(c.status))+' · '+dateTime(c.updated_at)+'</p>'+(c.payload.decision_note?'<p>'+esc(c.payload.decision_note)+'</p>':"")+'</article>';}).join("");html+=(state.data.commissions||[]).map(function(c){return '<article class="pcr-job"><b>'+esc(c.model)+' · '+esc(c.jobId)+'</b><p>'+money(c.commission)+' · '+esc(label(c.status))+'</p><p>'+dateTime(c.paidAt||c.approvedAt||c.earnedAt)+'</p>'+(c.payout_reference?'<p>อ้างอิงการจ่าย '+esc(c.payout_reference)+'</p>':'')+'</article>';}).join('');$('[data-activity]',root).innerHTML=html||empty("คำขอและรายการรายได้จะแสดงที่นี่");$('[data-console-history]',root).innerHTML=changes.filter(function(c){return c.action==="console_request";}).map(function(c){return '<article class="pcr-job"><p>'+esc(c.payload.message)+'</p><p>'+esc(label(c.status))+'</p>'+(c.payload.decision_note?'<p>ทีม MMD: '+esc(c.payload.decision_note)+'</p>':"")+'</article>';}).join("");}
  function renderPerformance(){var models=state.data.models||[];var jobs=state.data.jobs||[],ledger=state.data.commissions||[];$('[data-performance]',root).innerHTML=models.map(function(m){var mine=jobs.filter(function(j){return j.model_record_id===m.model_record_id;});var rows=ledger.filter(function(c){return c.model_record_id===m.model_record_id;});return '<article class="pcr-section"><h3>'+esc(m.display_name)+'</h3><dl class="pcr-stat-list"><div><dt>งานทั้งหมด</dt><dd>'+mine.length+'</dd></div><div><dt>ยืนยันแล้ว</dt><dd>'+mine.filter(function(j){return j.status==="confirmed";}).length+'</dd></div><div><dt>ค่าตอบแทนบันทึกแล้ว</dt><dd>'+money(rows.filter(function(c){return c.included_in_earnings;}).reduce(function(s,c){return s+Number(c.commission||0);},0))+'</dd></div></dl></article>';}).join("")||empty("ผลงานจะแสดงเมื่อมีข้อมูลที่ยืนยันแล้ว");}
  function openOperationsModel(model,form){
    var proposal=model.sales_control&&model.sales_control.proposal||{};form.elements.schedule_type.value=proposal.schedule_type||"Always";form.elements.effective_from_at.value=localInput(proposal.effective_from_at);form.elements.effective_until_at.value=localInput(proposal.effective_until_at);form.elements.availability_note.value=model.availability_note||"";
    var w=model.working_system||{};$('[data-current-working]',root).textContent=workingText(w);form.elements.working_system.value=w.system||"bridge";form.elements.commission_percent.value=w.commission_percent||"";form.elements.source_rate_thb.value=w.source_rate_thb||"";form.elements.partner_share_percent.value=w.partner_share_percent||"";
    var preview=model.sales_control&&model.sales_control.policy_preview;$('[data-sales-preview]',root).innerHTML=preview?'<h4>นโยบายขาย ณ '+dateTime(preview.requested_at)+'</h4><p>แยกตามกลุ่มลูกค้า · สิทธิ์ของลูกค้าจริงยังตรวจตอนเลือกงาน</p>'+preview.audiences.map(function(a){return '<p>'+esc(a.audience)+' · '+(a.sellable?'เปิดตามนโยบาย':'ปิด / ไม่มีเรทที่ใช้ได้')+' · '+(a.rate_thb==null?'รอตรวจราคา':money(a.rate_thb))+'</p>';}).join(''):'';
    var p=state.vault&&state.vault.models&&state.vault.models[model.model_record_id]||{};["line","telegram","availability"].forEach(function(k){form.elements["private_"+k].value=p[k]||"";form.elements["private_"+k].disabled=!state.vault;});
    var jobs=(state.data.jobs||[]).filter(function(j){return j.model_record_id===model.model_record_id;});var ledger=(state.data.commissions||[]).filter(function(c){return c.model_record_id===model.model_record_id;});var external=privateEvents().filter(function(e){return e.model_record_id===model.model_record_id&&e.kind==="external";});
    $('[data-model-history]',root).innerHTML=jobs.map(function(j){return '<p>MMD · '+dateTime(j.start_at||j.date)+' · '+esc(label(j.status))+'</p>';}).concat(ledger.map(function(c){return '<p>'+esc(c.jobId)+' · '+money(c.commission)+' · '+esc(label(c.status))+'</p>';}),external.map(function(e){return '<p>งานภายนอก · '+dateTime(e.start_at)+' · '+money(e.income)+' · '+esc(label(e.payment))+'</p>';})).join("")||empty("ประวัติจะแสดงเมื่อมีรายการ");loadAssets(model.model_record_id);
  }
  function savePrivateModel(){if(!ensurePrivate()||!state.model)return;var f=$('[data-model-form]',root);state.vault.models=state.vault.models||{};var prior=state.vault.models[state.model.model_record_id]||{};state.vault.models[state.model.model_record_id]=Object.assign({},prior,{line:f.elements.private_line.value,telegram:f.elements.private_telegram.value,availability:f.elements.private_availability.value});state.vault.model_notes[state.model.model_record_id]=f.elements.private_note.value;saveVault().then(function(){renderContacts();}).catch(function(e){setFlash(e.message,"error");});}
  function archivePrivateModel(){if(!ensurePrivate()||!state.model)return;state.vault.models=state.vault.models||{};var p=state.vault.models[state.model.model_record_id]||{};p.archived=!p.archived;state.vault.models[state.model.model_record_id]=p;saveVault().then(function(){$('[data-model-dialog]',root).close();filterRoster();}).catch(function(e){setFlash(e.message,"error");});}
  async function encryptVaultPayload(payload,pin,salt){var iv=crypto.getRandomValues(new Uint8Array(12)),key=await vaultKey(pin,salt);var cipher=await crypto.subtle.encrypt({name:"AES-GCM",iv:iv},key,new TextEncoder().encode(JSON.stringify(payload)));return {version:1,salt:b64(salt),iv:b64(iv),ciphertext:b64(new Uint8Array(cipher))};}
  async function importVaultBackup(){
    if(!ensurePrivate())return;var file=$('[data-import-vault-file]',root).files[0],pin=$('[data-import-vault-pin]',root).value,button=$('[data-import-vault]',root);
    if(!file||file.size>1000000||pin.length<8)return setFlash("เลือกไฟล์สำรองไม่เกิน 1 MB และใส่ PIN ของไฟล์","error");
    button.disabled=true;var previous=null;
    try {
      var envelope=JSON.parse(await file.text());if(envelope.version!==1||![envelope.salt,envelope.iv,envelope.ciphertext].every(function(v){return typeof v==='string'&&/^[A-Za-z0-9_-]+$/.test(v);})||unb64(envelope.salt).length!==16||unb64(envelope.iv).length!==12)throw Error("ไฟล์สำรองต้องใช้รูปแบบที่ระบบรองรับ");
      var key=await vaultKey(pin,unb64(envelope.salt));var plain=await crypto.subtle.decrypt({name:"AES-GCM",iv:unb64(envelope.iv)},key,unb64(envelope.ciphertext));var restored=JSON.parse(new TextDecoder().decode(plain));
      if(state.authBlocked)return;
      if(!restored||restored._partner_record_id!==state.data.partner.id||!restored.travel_notes||!restored.model_notes||typeof restored.general_note!=='string'||(restored.events&&!Array.isArray(restored.events)))throw Error("ไฟล์นี้ไม่ตรงกับ Partner หรือรูปแบบ Vault");
      if(!confirm("กู้คืนข้อมูลจากไฟล์นี้แทน Vault ปัจจุบัน? กรุณาสำรองข้อมูลปัจจุบันก่อน"))return;
      await state.vaultQueue.catch(function(){});if(state.vaultConflict)throw Error("Vault ขัดแย้งกับอีกอุปกรณ์ กรุณาสำรองข้อมูลแล้วล็อกและโหลดใหม่");
      previous=state.vault;state.vault=restored;$('[data-general-note]',root).value=restored.general_note;await saveVault();hydrate(state.data);setFlash("กู้คืนและเข้ารหัส Vault ด้วย PIN ปัจจุบันแล้ว","success");
    }catch(e){if(previous){state.vault=previous;$('[data-general-note]',root).value=previous.general_note||'';}setFlash("กู้คืนไม่สำเร็จ: "+e.message,"error");}
    finally{$('[data-import-vault-pin]',root).value='';$('[data-import-vault-file]',root).value='';button.disabled=false;}
  }
  function lockVault(){$('[data-import-vault-pin]',root).value="";$('[data-import-vault-file]',root).value="";state.vaultReady=false;state.vault=null;state.vaultPin="";state.vaultSalt=null;$('[data-vault-pin]',root).value="";$('[data-general-note]',root).value="";$('[data-vault-open]',root).hidden=true;$('[data-vault-locked]',root).hidden=false;$('[data-model-dialog]',root).close();$('[data-event-dialog]',root).close();$('[data-event-form]',root).reset();$$('[name^=private_]',root).forEach(function(el){el.value='';});$('[data-model-history]',root).textContent='';hydrate(state.data);}
  function renderContacts(){var target=$('[data-model-contacts]',root);if(!state.vault){target.innerHTML=empty("ปลดล็อก Private Vault เพื่อดูช่องติดต่อโมเดล");return;}target.innerHTML='<h3>ช่องติดต่อส่วนตัวของโมเดล</h3>'+(state.data.models||[]).map(function(m){var p=state.vault.models&&state.vault.models[m.model_record_id]||{};var tg=String(p.telegram||"").replace(/^@/,"");return '<article class="pcr-job"><b>'+esc(m.display_name)+'</b><p>LINE: '+esc(p.line||"รอเพิ่มข้อมูล")+'</p>'+( /^[a-zA-Z][a-zA-Z0-9_]{4,31}$/.test(tg)?'<a href="https://t.me/'+encodeURIComponent(tg)+'" target="_blank" rel="noreferrer noopener">เปิด Telegram ของโมเดล</a>':'<p>Telegram: '+esc(p.telegram||"รอเพิ่มข้อมูล")+'</p>')+'</article>';}).join("");}
  function submitWorking(){var form=$('[data-model-form]',root);if(!form.elements.share_with_mmd.checked)return setFlash("ยืนยัน Share with MMD ก่อนส่งข้อตกลง","error");var b=$('[data-submit-working]',root);b.disabled=true;api("/v1/partner/working-system",{method:"POST",headers:{"content-type":"application/json","Idempotency-Key":uid("working_")},body:JSON.stringify({model_record_id:state.model.model_record_id,system:form.elements.working_system.value,commission_percent:form.elements.commission_percent.value,source_rate_thb:form.elements.source_rate_thb.value,partner_share_percent:form.elements.partner_share_percent.value,change_reason:form.elements.working_reason.value,share_with_mmd:true})}).then(function(){setFlash("ส่งข้อตกลงให้ Boss Per ตรวจแล้ว","success");return load();}).catch(function(e){setFlash(e.message,"error");}).finally(function(){b.disabled=false;});}
  function submitConsole(event){event.preventDefault();var f=event.currentTarget,b=f.querySelector('button[type="submit"]');if(!f.elements.share_with_mmd.checked)return;b.disabled=true;api("/v1/partner/models/change",{method:"POST",headers:{"content-type":"application/json","Idempotency-Key":uid("console_")},body:JSON.stringify({action:"console_request",session_record_id:f.elements.session_record_id.value,message:f.elements.message.value,share_with_mmd:true})}).then(function(){f.reset();setFlash("บันทึกคำขอประสานงานแล้ว","success");return load();}).catch(function(e){setFlash(e.message,"error");}).finally(function(){b.disabled=false;});}
  function exportCalendar(){exportRows("sigil-calendar.csv",[["โมเดล","แหล่งงาน","เริ่ม","สิ้นสุด","พื้นที่","สถานะ"]].concat((state.data.jobs||[]).filter(calendarMatch).map(function(j){return[j.model_name,"MMD",localInput(j.start_at),localInput(j.end_at),j.location,label(j.status)];}),privateEvents().filter(calendarMatch).map(function(e){var m=modelById(e.model_record_id);return[m?m.display_name:"",privateKinds[e.kind],localInput(e.start_at),localInput(e.end_at),e.area,label(e.status)];})));}
  function renderModelCovers(){
    coverBlobs.forEach(function(url){URL.revokeObjectURL(url);});coverBlobs=[];
    var generation=state.data;
    api("/v1/partner/models/assets").then(function(payload){
      var chosen={};(payload.assets||[]).filter(function(a){return a.approved_cover&&a.preview_available;}).sort(function(a,b){return Date.parse(b.decision_at||b.uploaded_at)-Date.parse(a.decision_at||a.uploaded_at);}).forEach(function(a){if(!chosen[a.model_record_id])chosen[a.model_record_id]=a;});
      Object.values(chosen).forEach(function(a){fetch('/v1/partner/models/asset?asset_id='+encodeURIComponent(a.asset_id)+'&t='+encodeURIComponent(token),{referrerPolicy:"no-referrer",cache:"no-store",credentials:"omit"}).then(function(r){if(!r.ok)throw Error();return r.blob();}).then(function(blob){if(generation!==state.data)return;var target=$('[data-model="'+a.model_record_id+'"] .pcr-model-image',root);if(!target)return;var url=URL.createObjectURL(blob);coverBlobs.push(url);var img=document.createElement('img');img.alt='';img.src=url;target.replaceChildren(img);}).catch(function(){});});
    }).catch(function(){});
  }
  function releaseAssets(){assetBlobs.forEach(function(url){URL.revokeObjectURL(url);});assetBlobs=[];}
  function loadAssets(modelId){releaseAssets();var target=$('[data-model-assets]',root);target.textContent="กำลังโหลดรูป";api("/v1/partner/models/assets").then(function(payload){if(!state.model||state.model.model_record_id!==modelId)return;var assets=(payload.assets||[]).filter(function(a){return a.model_record_id===modelId;});target.innerHTML=assets.length?assets.map(function(a){return '<article class="pcr-asset" data-asset="'+esc(a.asset_id)+'">'+(a.preview_available?'<img alt="'+esc(a.file_name)+'" data-asset-image>':"")+'<p>'+esc(a.file_name)+' · '+esc(label(a.review_status))+'</p>'+(a.preview_available?'<button type="button" data-asset-action="set_cover">เสนอเป็นภาพหลัก</button>':'')+'<button type="button" data-asset-action="'+(a.review_status==='archived'?'restore_asset':'archive_asset')+'">'+(a.review_status==='archived'?'ขอคืนรูปจากประวัติ':'ขอเก็บเข้าประวัติ')+'</button></article>';}).join(""):empty("ยังไม่มีไฟล์ที่แชร์จากหน้านี้");assets.filter(function(a){return a.preview_available;}).forEach(function(a){fetch('/v1/partner/models/asset?asset_id='+encodeURIComponent(a.asset_id)+'&t='+encodeURIComponent(token),{referrerPolicy:"no-referrer",cache:"no-store",credentials:"omit"}).then(function(r){if(!r.ok)throw Error();return r.blob();}).then(function(blob){if(!state.model||state.model.model_record_id!==modelId)return;var img=$('[data-asset="'+a.asset_id+'"] [data-asset-image]',target);if(!img)return;var url=URL.createObjectURL(blob);assetBlobs.push(url);img.src=url;}).catch(function(){});});$$('[data-asset-action]',target).forEach(function(b){b.onclick=function(){var f=$('[data-model-form]',root);if(!f.elements.share_with_mmd.checked)return setFlash("ยืนยัน Share with MMD ก่อนส่งคำขอรูป","error");b.disabled=true;api("/v1/partner/models/change",{method:"POST",headers:{"content-type":"application/json","Idempotency-Key":uid("asset_")},body:JSON.stringify({action:b.dataset.assetAction,asset_id:b.closest('[data-asset]').dataset.asset,model_record_id:modelId,share_with_mmd:true})}).then(function(){setFlash("ส่งคำขอรูปให้ MMD ตรวจแล้ว","success");return load();}).catch(function(e){setFlash(e.message,"error");}).finally(function(){b.disabled=false;});};});}).catch(function(){target.textContent="โหลดรูปไม่สำเร็จ ลองเปิดโมเดลอีกครั้ง";});}
  installOperations();
`;

export const PARTNER_OPERATIONS_CSS = String.raw`
[data-partner-control-room] [hidden]{display:none!important}.pcr-toolbar{display:flex;flex-wrap:wrap;align-items:end;gap:12px;margin:16px 0}.pcr-toolbar label{display:grid;gap:6px;font-size:12px;color:var(--mmdp-muted)}.pcr-toolbar input,.pcr-toolbar select,[data-console-form] select,[data-console-form] textarea{max-width:100%;padding:12px;border:1px solid var(--mmdp-line);border-radius:12px;background:#0c0b09;color:#fff;font:inherit}.pcr-toolbar button,[data-partner-control-room] .pcr-section button,.pcr-section-head button,.pcr-job button,.pcr-vault-actions button{padding:10px 15px;min-height:44px;border:1px solid var(--mmdp-line);border-radius:12px;background:#251e13;color:var(--mmdp-gold-2);font:inherit;cursor:pointer}[data-partner-control-room] button:disabled{opacity:.5;cursor:wait}[data-partner-control-room] button:focus-visible,[data-partner-control-room] a:focus-visible{outline:2px solid #f2d196;outline-offset:3px}.pcr-collision{padding:12px;border-left:3px solid #ec765d;background:#391c14;color:#ffc7b2}.pcr-stat-list{display:flex;flex-wrap:wrap;gap:20px}.pcr-stat-list dt{font-size:12px;color:var(--mmdp-muted)}.pcr-stat-list dd{margin:8px 0;font-size:22px;color:#fff}.pcr-section+.pcr-section{margin-top:14px}.pcr-asset{padding:14px 0;border-bottom:1px solid var(--mmdp-line)}.pcr-asset img{width:120px;height:150px;object-fit:cover;border-radius:12px}.pcr-asset button{margin:5px}[data-console-form]{display:grid;gap:16px}[data-console-form] label{display:grid;gap:8px}[data-console-form] textarea{min-height:120px}.pcr-form .pcr-checks input{width:auto}.pcr-form fieldset label{margin:10px 0}.pcr-form fieldset>button{margin:6px 0}.pcr-privacy{font-size:13px}body[data-mmd-partner-page="partner-dashboard"]:has([data-partner-control-room]) .mmdp-hero{padding:22px 0;min-height:auto}body[data-mmd-partner-page="partner-dashboard"]:has([data-partner-control-room]) .mmdp-hero h1{font-size:32px}body[data-mmd-partner-page="partner-dashboard"]:has([data-partner-control-room]) .mmdp-hero aside,body[data-mmd-partner-page="partner-dashboard"]:has([data-partner-control-room]) .mmdp-grid{display:none}body[data-mmd-partner-page="partner-dashboard"]:has([data-partner-control-room]) .mmdp-hero{grid-template-columns:1fr}@media(max-width:600px){.pcr-toolbar{display:grid;grid-template-columns:1fr 1fr}.pcr-toolbar label{min-width:0}.pcr-toolbar input,.pcr-toolbar select{width:100%;min-width:0}.pcr-section-head{align-items:flex-start;flex-direction:column}.pcr-job-main{flex-wrap:wrap}.pcr-side{max-width:calc(100vw - 8px)}}@media print{body{background:#fff!important;color:#111!important}.mmdp-nav,.mmdp-hero,.mmdp-grid,.pcr-side,.pcr-privacy,.pcr-toolbar,.pcr-flash,.pcr-main>section:not([data-view-panel="earnings"]){display:none!important}.pcr-shell{display:block}.pcr-section,.pcr-table td,.pcr-table th{color:#111!important;background:#fff!important;border-color:#aaa!important}.pcr-top h2{color:#111!important}.pcr-table table{min-width:0}a[href]:after{content:none!important}}
`;
